

Poetry in use
```
python3 -m venv <name>
poetry config virtualenvs.in-project false
poetry config virtualenvs.create false
poetry install
```

Devices info located in `data` folder(shuld be renamed in future)
